version: '3.3'
services:
  zoraclebotdev:
    container_name: zoracleBotDev
    restart: always
    build: .
    volumes:
      - ./src:/usr/src/app
      #- /usr/src/app/node_modules
    command: npm run developmentNodemon
