version: '3.3'
services:
  zoraclebot:
    container_name: zoracleBot
    restart: always
    build: .
    volumes:
      - ./src:/usr/src/app
      #- /usr/src/app/node_modules
    command: npm run production
